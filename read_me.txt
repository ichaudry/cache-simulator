To compile the source files please run the following commands:

gcc sys1.c -o sys1 -lm

gcc sys2.c -o sys2 -lm

The generate executables can be run using ./sys1 and ./sys2

The programs will prompt for trace file, cache size etc.